import React from 'react'

export default function Person() {
  return (
    <div>
        <h2>{ "ID number" + person.id + " is " + person.name + " aged " + person.age + " and knows " + person.skill}</h2>
    </div>
  )
}
